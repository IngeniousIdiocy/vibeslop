// Build metadata injected at build time
declare const __BUILD_METADATA__: {
  buildNumber: number;
  commitSha: string;
  commitShort: string;
  branch: string;
  timestamp: string;
  version: string;
};

