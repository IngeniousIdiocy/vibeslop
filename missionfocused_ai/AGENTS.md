# Mission-Focused AI Instructions

This subproject contains a **single-file, single-page** web application. All HTML, CSS, JavaScript, and assets are embedded directly into one HTML file. No additional pages or external assets should be created. When editing or generating the application, always produce exactly **one** HTML file.

Currently this subproject has no automated tests. The `tests/` directory is reserved for future use.
